#include "parameters.h"

/* All I&F neurons have the same threshold, reset and resting potentials.
 * Define them as constants */
const double V_L  = 0.0;    /* resting potential */
const double V_thr   = 20.0; /* voltage threshold */
const double V_reset = 10.0; /* reset potential */
